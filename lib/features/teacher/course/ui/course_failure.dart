import 'package:flutter/material.dart';

class CourseFailure extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Failure'),
    );
  }
}
