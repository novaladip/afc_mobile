import 'package:flutter/material.dart';

class SectionUpdate extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Section Detail Update"),
    );
  }
}
